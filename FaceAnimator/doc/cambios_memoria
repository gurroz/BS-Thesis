Cambios

-	Se comento la libreria #include <sigc++/compatibility.h>
-	Se cambiaron las apariciones de SigC::slot por sigc::mem_fun y de SigC:: por sigc::
- 	Se agregó la libreria #include <limits> cuando decia que numeric_limits no definida
-	Se agrego #include <stdio.h> cuando decia fopen no declarado
-	Se agregaron los includes:
#include <vector>
#include <iterator>
#include <algorithm>
#include <iostream>

	cuando tiraba error por find.
- Cuando reclamaba por strcmp se agrego #include <stdio.h>
					#include <string.h>

- Se arreglo el error al momento de abrir un malla siempre mostraba el menu de registration (hacer calzar las caras)
- Saca copiar, pegar, cortar
- Poner informacion en ver


Cambios que hay que hacer:

- Agregar distribuir hormona *
- Action units debiese ir en malla
-existe la clase MovimientoNodos, la cual se debe eliminar y colgar  sus hijos directamente de
AlgDesplazamientoMallaTriangulos, ya que actualmente no se encuentra ligada a ninguna clase, y efectivamente pertenece a un tipo de movimiento de nodos
- llamada GenerarMallaInicial, el cual contendrá como hijos a las clases RegistrationMethod e Interpolator, ya que actualmente estas por si solas no permiten identificar su dependencia dentro del diagrama ni su uso.
- En deformar hay que agregar lo de calzar caras (registracionMethod, ahi mismo hace la interpolacion). 




